import React from 'react';
import { solutions, allowedWords, allInputs, valuesMap, addWord2, scoreWordBruteForce2, scoreInputsBruteForce, getSolutionsAfterWord} from '../../utils/solver';

const CurrentGame = props => {
    let info = []
    let currentSolutions = allInputs
    let attempts = 0

    return (
        <React.Fragment>
            <h1>Current Game Solver</h1>
        </React.Fragment>
    )
}

export default CurrentGame;